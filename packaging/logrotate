/var/log/gridbeat/*.log {
    daily
    rotate 7
    compress
    missingok
    dateext
    dateformat .%Y%m%d
    sharedscripts
    postrotate
        kill -USR1 `cat /run/gridbeat.pid`
    endscript
}
